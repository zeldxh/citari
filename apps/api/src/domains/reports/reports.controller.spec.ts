import{describe,it,vi}from"vitest";import{ReportsController}from"./reports.controller.js";describe("ReportsController",()=>{it("delegates all reports using the JWT tenant",async()=>{const reports={dashboard:vi.fn(),agenda:vi.fn(),detail:vi.fn(),demand:vi.fn(),availability:vi.fn()};const c=new ReportsController(reports as never),r={principal:{userId:"u",tenantId:"t"}} as never,q={from:"2026-01-01",to:"2026-01-02"};await c.dashboard(r);await c.agenda(r,q);await c.detail(r,q);await c.demand(r,q);await c.availability(r);});});
