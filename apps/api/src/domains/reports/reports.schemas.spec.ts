import{describe,expect,it}from"vitest";import{rangeSchema}from"./reports.schemas.js";describe("report range",()=>{it("parses bounded ranges",()=>expect(rangeSchema.parse({from:"2026-01-01",to:"2026-01-31"}).page).toBe(1));it("rejects inverted ranges",()=>expect(()=>rangeSchema.parse({from:"2026-02-01",to:"2026-01-01"})).toThrow());it("rejects excessive ranges",()=>expect(()=>rangeSchema.parse({from:"2020-01-01",to:"2026-01-01"})).toThrow());});
